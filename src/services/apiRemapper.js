/**
 * API Remapper
 *
 * Utility for remapping API responses and requests
 */

export function remapResponse(data) {
  return data;
}

export function remapRequest(data) {
  return data;
}

export function remapData(data) {
  return data;
}

export default {
  remapResponse,
  remapRequest,
  remapData
};
