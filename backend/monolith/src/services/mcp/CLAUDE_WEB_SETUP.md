# Подключение Integram MCP к Claude

## ⚠️ ВАЖНО: Веб-версия claude.ai НЕ поддерживает кастомные MCP серверы

**Страница claude.ai Settings → Connectors** показывает только официальные предустановленные коннекторы от Anthropic.

Добавить туда свой MCP сервер через URL **невозможно**.

## ✅ Доступные способы подключения

### Способ 1: Claude Desktop (Рекомендуется) ⭐

**Claude Desktop** (настольное приложение) поддерживает кастомные MCP серверы.

#### Установка Claude Desktop:

1. Скачайте: https://claude.ai/download
2. Установите на ваш компьютер (Windows/macOS/Linux)

#### Настройка:

**macOS:**
```bash
mkdir -p ~/Library/Application\ Support/Claude
nano ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

**Windows (PowerShell):**
```powershell
$configPath = "$env:APPDATA\Claude\claude_desktop_config.json"
New-Item -ItemType File -Path $configPath -Force
notepad $configPath
```

**Linux:**
```bash
mkdir -p ~/.config/Claude
nano ~/.config/Claude/claude_desktop_config.json
```

**Содержимое файла конфигурации:**

```json
{
  "mcpServers": {
    "integram": {
      "command": "node",
      "args": [
        "/ABSOLUTE/PATH/TO/dronedoc2025/backend/monolith/src/services/mcp/http-mcp-bridge.js"
      ],
      "env": {
        "MCP_SERVER_URL": "https://dev.drondoc.ru",
        "MCP_API_PATH": "/api/mcp/integram"
      }
    }
  }
}
```

**⚠️ Замените** `/ABSOLUTE/PATH/TO/dronedoc2025` на реальный абсолютный путь к проекту.

**Для Windows** пример пути:
```json
"args": ["C:\\Users\\YourName\\dronedoc2025\\backend\\monolith\\src\\services\\mcp\\http-mcp-bridge.js"]
```

#### Использование:

1. Перезапустите Claude Desktop
2. Создайте новый чат
3. Спросите: "Какие MCP серверы подключены?"
4. Должен отобразиться "integram" с 27 инструментами

### Способ 2: Claude Code CLI

**Claude Code CLI** уже настроен в `.claude/mcp.json` в корне проекта.

Используйте команду `claude` в терминале для работы с MCP.

### Способ 3: Прямое использование HTTP API

Если Claude Desktop недоступен, используйте HTTP API напрямую:

```bash
# Список инструментов
curl https://dev.drondoc.ru/api/mcp/integram/tools

# Выполнение команды
curl -X POST https://dev.drondoc.ru/api/mcp/integram/execute \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "integram_authenticate",
    "arguments": {
      "serverURL": "https://dronedoc.ru",
      "database": "a2025",
      "login": "d",
      "password": "d"
    }
  }'
```

### Способ 4: Веб-интерфейс на DronDoc.ru

Планируется создание веб-интерфейса для визуальной работы с Integram MCP через браузер.

---

## 🎯 Что даёт подключение MCP?

После подключения через Claude Desktop или Claude Code вы сможете:

- ✅ Создавать типы (таблицы) и реквизиты (колонки)
- ✅ Добавлять, редактировать и удалять объекты (записи)
- ✅ Выполнять запросы к базе данных
- ✅ Получать метаданные о структуре БД
- ✅ Управлять всеми 27 инструментами Integram API

Всё это через естественный язык, без необходимости писать API запросы вручную.

## 📝 Пример использования в Claude Desktop

После настройки в любом чате:

```
Привет! Подключись к Integram:
- Сервер: https://dronedoc.ru
- База данных: a2025
- Логин: d
- Пароль: d
```

Claude автоматически вызовет инструмент `integram_authenticate`.

Затем можете делать запросы:

```
Покажи все типы в базе данных
```

```
Создай новый тип "Проекты" с реквизитами "Название" и "Описание"
```

```
Покажи все объекты в типе "Проекты"
```

## 📋 Подробная инструкция

### Доступные endpoints

MCP сервер предоставляет следующие HTTP endpoints:

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/mcp/integram/manifest` | GET | Manifest файл для claude.ai |
| `/api/mcp/integram/tools` | GET | Список всех 27 инструментов |
| `/api/mcp/integram/execute` | POST | Выполнение инструмента |

### Manifest endpoint (для разработчиков)

Manifest предоставляет метаинформацию о сервере:

```bash
curl https://dev.drondoc.ru/api/mcp/integram/manifest
```

**Ответ:**
```json
{
  "schema_version": "1.0",
  "name": "Integram MCP Server",
  "description": "MCP server for Integram database operations",
  "version": "1.0.0",
  "capabilities": {
    "tools": true
  },
  "endpoints": {
    "tools": "https://dev.drondoc.ru/api/mcp/integram/tools",
    "execute": "https://dev.drondoc.ru/api/mcp/integram/execute"
  },
  "authentication": {
    "type": "none",
    "note": "Authentication is handled per-tool via integram_authenticate"
  }
}
```

### Tools endpoint

Получить список всех инструментов:

```bash
curl https://dev.drondoc.ru/api/mcp/integram/tools
```

**Ответ:**
```json
{
  "success": true,
  "tools": [
    {
      "name": "integram_authenticate",
      "description": "Authenticate with Integram API and establish a session",
      "inputSchema": { ... }
    },
    ...
  ],
  "count": 27
}
```

### Execute endpoint

Выполнить инструмент:

```bash
curl -X POST https://dev.drondoc.ru/api/mcp/integram/execute \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "integram_authenticate",
    "arguments": {
      "serverURL": "https://dronedoc.ru",
      "database": "a2025",
      "login": "d",
      "password": "d"
    }
  }'
```

**Ответ:**
```json
{
  "success": true,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\"success\":true,\"token\":\"...\",\"userId\":1,...}"
      }
    ]
  },
  "toolName": "integram_authenticate"
}
```

## 🛠️ Примеры использования

### Пример 1: Аутентификация и получение типов

**Вы (в чате с Claude):**
```
Подключись к Integram на https://dronedoc.ru, база a2025, логин d, пароль d.
Затем покажи все типы в базе.
```

**Claude выполняет:**
1. Вызывает `integram_authenticate` с указанными параметрами
2. Вызывает `integram_get_dictionary` для получения списка типов
3. Отображает результат в удобном формате

**Результат:**
```
Подключение установлено успешно!

Найдено 15 типов в базе данных:

1. Пользователи (ID: 18)
2. Проекты (ID: 42)
3. Задачи (ID: 43)
...
```

### Пример 2: Создание нового объекта

**Вы:**
```
Создай новый проект с названием "Тестовый проект 2025"
```

**Claude:**
1. Ищет тип "Проекты" (используя кэш или `integram_get_dictionary`)
2. Вызывает `integram_create_object` с typeId=42 и value="Тестовый проект 2025"
3. Подтверждает создание

**Результат:**
```
Объект создан успешно:
- ID: 1234
- Название: Тестовый проект 2025
- Тип: Проекты (42)
```

### Пример 3: Добавление реквизита к типу

**Вы:**
```
Добавь к типу "Проекты" новый реквизит "Бюджет" типа NUMBER
```

**Claude:**
1. Получает metadata типа "Проекты" (`integram_get_type_metadata`)
2. Вызывает `integram_add_requisite` с typeId=42 и requisiteTypeId=13 (NUMBER)
3. Подтверждает добавление

## 📊 Доступные инструменты (27 штук)

### 🔐 Аутентификация (2)

| Инструмент | Описание |
|------------|----------|
| `integram_authenticate` | Аутентификация с логином/паролем |
| `integram_set_context` | Установка контекста из токена |

### 📐 DDL операции - структура БД (8)

| Инструмент | Описание |
|------------|----------|
| `integram_create_type` | Создать тип (таблицу) |
| `integram_save_type` | Сохранить свойства типа |
| `integram_delete_type` | Удалить тип |
| `integram_add_requisite` | Добавить реквизит (колонку) |
| `integram_delete_requisite` | Удалить реквизит |
| `integram_save_requisite_alias` | Сохранить алиас реквизита |
| `integram_toggle_requisite_null` | Переключить NULL constraint |
| `integram_toggle_requisite_multi` | Переключить multiselect |

### 📝 DML операции - данные (6)

| Инструмент | Описание |
|------------|----------|
| `integram_create_object` | Создать объект (запись) |
| `integram_save_object` | Сохранить объект |
| `integram_set_object_requisites` | Установить реквизиты |
| `integram_delete_object` | Удалить объект |
| `integram_move_object_up` | Переместить объект вверх |
| `integram_move_object_to_parent` | Переместить к другому parent |

### 🔍 Запросы (7)

| Инструмент | Описание |
|------------|----------|
| `integram_get_dictionary` | Получить словарь типов |
| `integram_get_type_metadata` | Метаданные типа |
| `integram_get_object_list` | Список объектов |
| `integram_get_object_edit_data` | Данные для редактирования |
| `integram_get_type_editor_data` | Данные редактора типов |
| `integram_execute_report` | Выполнить отчёт |
| `integram_get_reference_options` | Опции для dropdown |

### 🔢 Multiselect (2)

| Инструмент | Описание |
|------------|----------|
| `integram_add_multiselect_item` | Добавить элемент |
| `integram_remove_multiselect_item` | Удалить элемент |

### 📁 Файлы (2)

| Инструмент | Описание |
|------------|----------|
| `integram_get_dir_admin` | Содержимое директории |
| `integram_create_backup` | Создать бэкап |

## ❓ Часто задаваемые вопросы (FAQ)

### Q: Где вводить адрес сервера в claude.ai?

**A:** Settings → Connectors → Add Connector → Server URL: `https://dev.drondoc.ru/api/mcp/integram`

### Q: Какой адрес использовать?

**A:**
- **Основной (dev):** `https://dev.drondoc.ru/api/mcp/integram`
- **Production:** `https://drondoc.ru/api/mcp/integram`
- **Local:** `http://localhost:3000/api/mcp/integram` (для разработки)

### Q: Нужна ли аутентификация для подключения?

**A:** Нет, аутентификация происходит **внутри чата** через инструмент `integram_authenticate`. Сам MCP сервер доступен без авторизации.

### Q: Как проверить, что сервер работает?

**A:**
```bash
curl https://dev.drondoc.ru/api/mcp/integram/tools
```

Вы должны увидеть JSON с `"count": 27`.

### Q: Сколько инструментов доступно?

**A:** 27 инструментов:
- 2 для аутентификации
- 8 для DDL (структура БД)
- 6 для DML (данные)
- 7 для запросов
- 2 для multiselect
- 2 для файлов

### Q: Можно ли использовать с Claude Desktop?

**A:** Да! Для Claude Desktop используйте stdio конфигурацию (см. `CLAUDE_CODE_SETUP.md`).

### Q: Можно ли использовать с Claude Code CLI?

**A:** Да! Конфигурация уже готова в `.claude/mcp.json`.

### Q: Безопасно ли передавать пароли через Claude?

**A:**
- ⚠️ Используйте тестовые учётные записи или аккаунты с ограниченными правами
- ✅ Пароли передаются только при вызове `integram_authenticate`
- ✅ После аутентификации используется токен сессии
- ✅ Лучше использовать `integram_set_context` с готовым токеном

### Q: Что делать, если сервер не отвечает?

**A:**
1. Проверьте, работает ли orchestrator: `curl https://dev.drondoc.ru/api/health`
2. Проверьте доступность tools: `curl https://dev.drondoc.ru/api/mcp/integram/tools`
3. Проверьте логи сервера
4. Попробуйте альтернативный адрес (production или local)

## 🔧 Отладка

### Проверка доступности сервера

```bash
# Health check
curl https://dev.drondoc.ru/api/health

# Manifest
curl https://dev.drondoc.ru/api/mcp/integram/manifest

# Tools list
curl https://dev.drondoc.ru/api/mcp/integram/tools

# Test execute
curl -X POST https://dev.drondoc.ru/api/mcp/integram/execute \
  -H "Content-Type: application/json" \
  -d '{"toolName": "integram_authenticate", "arguments": {"serverURL": "https://dronedoc.ru", "database": "a2025", "login": "d", "password": "d"}}'
```

### Типичные ошибки

1. **"Cannot connect to server"**
   - Проверьте URL
   - Убедитесь, что сервер запущен
   - Попробуйте другой адрес (dev/production)

2. **"No tools available"**
   - Сервер работает, но integram-server не запускается
   - Проверьте логи: `/home/user/dronedoc2025/backend/monolith/logs/`

3. **"Authentication failed"**
   - Проверьте правильность логина/пароля
   - Убедитесь, что serverURL и database корректны

4. **"Tool execution timeout"**
   - Integram сервер недоступен
   - Сетевые проблемы между MCP и Integram

## 🎯 Итоговый чеклист

- [ ] Открыл claude.ai → Settings → Connectors
- [ ] Добавил сервер: `https://dev.drondoc.ru/api/mcp/integram`
- [ ] Вижу "Connected" и "27 tools available"
- [ ] Попросил Claude аутентифицироваться в Integram
- [ ] Успешно выполнил запрос (например, получил список типов)
- [ ] Протестировал создание объекта или другую операцию

## 📚 Дополнительные ресурсы

- [MCP для Claude Code (локальная установка)](./CLAUDE_CODE_SETUP.md)
- [HTTP MCP Usage](./HTTP_MCP_USAGE.md)
- [Integram MCP README](./INTEGRAM_MCP_README.md)
- [Примеры использования MCP клиента](../../examples/mcp-client-example.js)
- [Тесты Integram MCP сервера](./__tests__/integram-server.test.js)

## ✅ Готово!

Теперь вы можете использовать все возможности Integram API прямо из чата с Claude на claude.ai!

**Успешной работы с базами данных через естественный язык! 🚀**
